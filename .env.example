# Illuminate Conversational Intelligence - Environment Configuration
#
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# This file is used by deploy-base.sh to pass Snowflake credentials
# to the base CloudFormation stack, which stores them in AWS Secrets Manager.
# After the initial deployment, the secret persists and these values
# do not need to be re-supplied for subsequent stack updates.

# Snowflake credentials
SNOWFLAKE_ACCOUNT=your-account-identifier
SNOWFLAKE_USER=SVC_BLACKBOARD_DATA
SNOWFLAKE_PASSWORD=your-password
SNOWFLAKE_DATABASE=your-database
SNOWFLAKE_WAREHOUSE=BLACKBOARD_DATA_WH
SNOWFLAKE_ROLE=BBDATA_USER_ROLE

# Cognito initial user (created during first deploy)
COGNITO_USER_EMAIL=admin@example.com
COGNITO_USER_PASSWORD=YourSecurePassword123!
COGNITO_USER_NAME=Your Name

# AWS configuration (optional)
# AWS_REGION=us-east-1
