# Illuminate Conversational Intelligence - Python Dependencies

# Core
httpx>=0.25.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# LLM Clients
anthropic>=0.40.0

# Agent Framework
strands-agents>=1.0.0
strands-agents-tools>=0.2.0

# MCP (Model Context Protocol)
mcp>=1.0.0

# A2A (Agent-to-Agent Protocol)
a2a-sdk>=0.3.0

# AWS
boto3>=1.34.0

# Snowflake MCP server (installed separately via uvx, not pip)
# Run with: uvx snowflake-labs-mcp
# snowflake-connector-python is NOT needed - MCP server manages its own env

# API Server
fastapi>=0.109.0
uvicorn>=0.27.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.23.0
black>=24.1.0
ruff>=0.1.0
